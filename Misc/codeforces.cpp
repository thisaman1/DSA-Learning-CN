#include <bits/stdc++.h>
#define bg begin();
#define ed end();
#define ios ios_base::sync_with_stdio(0),cin.tie(0),cout.tie(0);
#define F first
#define S second
#define sit it=s.begin(); it !=s.end(); it++
#define all(x) x.begin(),x.end()
#define sz(x) (int)x.size()
#define alll(a) a  + 1,a + n  + 1
#define iti set <int> ::iterator it;
#define pp pop_back
#define pb push_back
#define lb(a, n, x) lower_bound (a , a + n , x) - a
#define ub(a, n, x) upper_bound (a , a + n , x) - a
typedef long long ll;
typedef long double ld;
typedef unsigned long long ull;
const long long  inf = (long long)1e18 + 879;
const int N = (long long)1e6 + 9;
using namespace std;